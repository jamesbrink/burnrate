{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default Burnrate desktop window permissions.",
  "windows": ["main", "tray"],
  "permissions": ["core:default", "core:event:default"]
}
